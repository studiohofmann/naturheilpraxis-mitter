import * as React from "react";
import Layout from "../components/layout/layout";

const Specialization = () => {
  return (
    <Layout pageTitle="Schwerpunkte">
      <p>Schwerpunkte</p>
    </Layout>
  );
};

export default Specialization;

export const Head = () => <title>Schwerpunkte</title>;
